MAQAM — HUMAN NARRATION FOR THE EXISTING 2:48 CHROME VIDEO

Read only the prose. Rehearse against Clipchamp and pause where needed. Do not speed up footage. This describes the existing recording, which does NOT show the later 0.10-USDC wallet transaction. Use your real voice.

[0:00–0:15 — Opening]
Maqam connects agent discovery to one exact payment authorization. The Graph supplies live agent data. Arc enforces the payment. A person decides what to approve, and the contract prevents the executor from changing those terms.

[0:15–0:45 — Discovery, threshold zero]
Here we query the Agent0 Subgraph on Base Sepolia for treasury work. Maqam screens the latest fifty registrations for an active profile, an explicit agent wallet, and a matching task. The page shows the indexed block and retrieval time. A match means review, not trust: descriptions are self-declared, and the registered owner is never substituted for a missing agent wallet.

[0:45–1:10 — Stricter screening]
Raising the minimum to five distinct reviewers holds this candidate. The Graph changes the decision, rather than just decorating the page. Stale indexing or query errors also stop discovery. Nobody has hired or paid this third-party agent in our demonstration.

[1:10–1:40 — Recorded model analysis]
This panel preserves a real model analysis of an earlier Graph snapshot. It recommends human review and explains the missing trust evidence. The model call is recorded; the discovery query was live. Its prompt, response, and supporting snapshot remain inspectable. A recommendation never gives the model authority to move funds.

[1:40–1:55 — Payment workspace]
For an agent payment, the person must verify the invoice and recipient. Review refreshes the Graph evidence. The signature binds its commitment together with the exact payment terms, chain, and contract.

[1:55–2:25 — Existing Arc proof]
Here is a separate, already-mined one-USDC test payment using controlled accounts. A scripted owner authorized it, and a different executor settled it. The browser verifies its receipt against live Arc RPC. Recorded simulations reject changed recipients, doubled amounts, and replay. The correct payment was actually mined. This fixture demonstrates execution, not payment for the discovered agent's services.

[2:25–2:48 — Architecture and closing]
The contract consumes the nonce once; cancellation and revocation can invalidate pending approvals. Thirty-two automated tests pass, and the participant's wallet has also completed a separate test payment. Maqam is a new ETHOnline application using a disclosed public library: an inspectable path from agent evidence to narrowly scoped authorization.

RECORDING REVIEW / WALLET FOOTAGE
The current silent export is 2:48, 1080p, 38.10 MB. Sampled opening, discovery, model, receipt and ending frames show Maqam; the browser toolbar and Codex are outside the crop. Black bands remain above and below the app. Check small proof text at full-screen playback. Human narration is still required. This is a sampled visual review, not a claim that every frame or final audio was inspected.

To SHOW the wallet flow, record a replacement Chrome segment. Do not narrate wallet clicks over the old fixture. Capture the connected address and Arc Testnet, exact amount/recipient review, actual approval/signing, settlement confirmation/link, and replay rejection. A fresh payment requires a fresh authorization and actual confirmation. Existing transaction 0x916fa842aafebf1caf3b0aee628601755f16acf9223c509bd0b975a7fb52d678 can instead be shown explicitly as an already-completed payment.

Optional replacement narration, ONLY with matching new footage:
"This is my connected MetaMask wallet on Arc Testnet. The terms authorize a 0.10 test-USDC payment back to my own address. The transaction has confirmed on Arc, and the nonce is consumed. Trying to reuse the authorization is blocked. This is a real browser-wallet self-payment, separate from the earlier demonstration with different owner and executor accounts."

Keep the final video between two and four minutes, at normal speed, with your human voice. Never show seed phrases, private keys, or unrelated wallet activity.
