MAQAM PROTOCOL — GRAPH + ARC DEMO
Human narration for real Chrome capture. Target 3:00–3:30; rehearse at normal speed.
Do not speak the bracketed actions. Silent footage needs your own voice added before submission.

[0:00 — Opening screen, then Find an agent.]
Maqam Protocol helps people discover agents and authorize one exact payment without handing over unrestricted wallet control. The Graph supplies live agent data. Arc enforces the payment.

[0:20 — Search treasury with minimum reviewers zero.]
This request queries The Graph's Agent0 Subgraph on Base Sepolia. The app screens the latest fifty registrations for an active profile, a registered agent wallet, and a match to the task. It shows the indexed block and retrieval time. This is live data, not a saved list.

The review decision is exploratory. An agent's description is self-declared, and feedback is not proof of trust. The app never substitutes the registration owner's address when an agent wallet is missing.

[0:55 — Change minimum reviewers to five and search again.]
A stricter policy changes the decision. A matching agent without enough distinct reviewers is held. Stale data and indexing errors also stop discovery. The Graph therefore changes what the app permits us to propose.

[1:20 — Click Inspect model reasoning from Graph evidence.]
This panel preserves a real model analysis of an earlier live Graph query. The model recommended a treasury candidate for human review and explained the missing trust evidence. That model call is recorded, not running while I speak. The prompt, response and Graph snapshot can all be inspected.

[1:50 — Workspace, show payment form. Do not select an unverified third-party invoice.]
Before paying a selected agent, the person must independently verify the invoice and recipient. Review refreshes the Graph data and binds the selected agent and exact snapshot hash into the payment evidence.

The signature also binds the owner, executor, token, amount, recipient, expiry, nonce and revocation epoch. The signing domain binds the chain and contract. The executor cannot change those terms.

[2:20 — Click Verify testnet payment. Wait for the live verification badge.]
Here is a separate, already-mined Arc Testnet payment using controlled test accounts. We did not hire or pay the third-party agent shown in discovery. A scripted owner authorized one test USDC, and a separate executor settled it.

The browser now verifies the receipt against live Arc RPC. Recorded checks show that changing the recipient or amount invalidates the signature, and replay fails after the nonce is consumed. The rejection checks were simulations; the correct payment was actually mined. Arc uses USDC for both payment and gas.

[3:05 — Show How it works.]
This new application was built during ETHOnline using the disclosed public Maqam library. It is a testnet prototype, not an audited production wallet. The result is an inspectable path from live agent data to a narrowly scoped payment authorization.

[Stop. Check the actual duration is 2–4 minutes, clear human voice, readable text, and no browser toolbar or Codex UI in the capture.]
