# Optional local proposal agent. Never put secrets in VITE_* variables.
AGENT_API_URL=https://api.openai.com/v1/chat/completions
AGENT_MODEL=
AGENT_API_KEY=
# Server-side Graph Studio key; never use a VITE_ prefix.
GRAPH_API_KEY=
# Optional separate key for the Ethereum Sepolia Agent0 deployment.
GRAPH_SEPOLIA_API_KEY=
